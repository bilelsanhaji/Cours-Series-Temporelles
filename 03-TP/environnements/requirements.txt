# Économétrie des séries temporelles — M1 MBFA
# Environnement Python.  Installation :  pip install -r requirements.txt
#
# Sur Google Colab, seuls `arch` et `pmdarima` manquent :
#     !pip install arch pmdarima

# <2.0 : pmdarima (voir plus bas) est distribué en wheel compilée contre
# l'ABI numpy 1.x ; avec numpy 2.x son import plante (ValueError sur la
# taille de numpy.dtype), aucune version de pmdarima ne corrige ça à ce jour.
numpy>=1.24,<2.0
pandas>=2.0
matplotlib>=3.7
scipy>=1.10

# Cœur économétrique : ARIMA, VAR, VECM, Johansen, tests de diagnostic
statsmodels>=0.14

# Racines unitaires (ADF, DF-GLS, Phillips-Perron, KPSS, Zivot-Andrews)
# et modèles de volatilité
arch>=6.2

# Équivalent de forecast::auto.arima
# Version épinglée : ce paquet a déjà connu des ruptures de compatibilité
# avec numpy et statsmodels en amont.
pmdarima>=2.0,<3.0

# Données externes (FRED, Yahoo Finance)
pandas-datareader>=0.10
yfinance>=0.2

# Notebooks
jupyter>=1.0
